# ArtDir — Cloudflare Pages config
# Deploy the web/ directory as a static site.
# No build step needed (plain HTML/CSS/JS).

[build]
  command = ""
  publish = "web"

[build.environment]
  # Set this to point to your self-hosted artdir server
  # ARTDIR_API_URL = "https://your-server.example.com"
